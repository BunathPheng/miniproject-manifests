---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: miniproject                  # Ingress name
  labels:
    app: miniproject
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "miniproject.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: miniproject               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
